# dsh-alert-sound — Roadmap / 功能计划书

> **核心定位**：让用户在**不盯着 dsh 画面**（在干别的事、切走窗口、全屏等）时，也能通过**声音 / 语音**及时知道 dsh 需要自己——尤其是不想工作被“卡住”。因此本插件的差异化是 **“免注视的声音/语音提醒”**，而非单纯“完成提示音”。

## 进度
- ✅ **A 播报具体内容**（语音带详情）
- ✅ **B 阻断事件重复提醒直到处理**
- ✅ **C 浏览器系统通知**
- ⬜ D 朗读输出 · E 卡住检测 · F/G/H/I（待做）

## 现状（v0.1，已发布）
- 4 类提醒：需要审批 / 需要回答 / 输出完成 / 发生错误。
- 每类：独立启停 + 音色（叮咚/低沉/轻点/警醒）+ 音量（0–200%）+ 试听。
- 可选**中文语音朗读**（固定语句，如“需要审批”）。
- 默认对所有会话提醒（可在设置切成“仅当前会话”）；后台标签页也能响（Web Audio + 语音合成）。
- 设置页（侧栏 → 提醒音）；设置存 localStorage（`dsh-alert-sound.v1`）。

---

## 待办功能（按“免注视 + 别卡住”价值排序）

### P0 — 高价值，直接命中目标

#### A. 语音播报“具体内容”（而不只是固定短句）
- **需求**：现在语音只读“需要审批/输出完成”等固定词。升级为播报**具体卡在哪**，让用户不看屏也知道是什么事。
- **播报内容**：
  - 审批 → `需要审批：<toolName>；<reason>`（例：写入文件 D:\xxx；运行命令 xxx）
  - 提问 → `需要回答：<问题文本>`
  - 错误 → `发生错误：<错误摘要>`
  - 完成 → `输出完成：<最后一句摘要>`
- **数据来源**：审批/提问取自 `pendingInteraction` 的 `payload`（toolName/reason / questions 文本）；错误/完成取自会话快照（`turn-error`、`assistant-step` 文本、`lastAgentError`）。
- **参考**：dsh-session-notification 的 `approvalText` / `questionText` / `sessionDetailOf` 已经能提取这些文本，直接借鉴。
- **验收**：触发审批时，语音能读出具 体工具名/原因；提问读到问题文本。
- **完善（A+）**：“完成”播报的详情目前是**原始最后回复**（可能长、被截断、信息冗余）。可改为让 **AI 生成一句简短、信息量高的摘要**专门用于播报（如“已生成三份文件”），或只读关键结论。

#### B. 阻断事件“重复提醒”，直到处理
- **需求**：审批/提问/错误会**卡住**工作。加“每 N 秒再响一次，直到用户处理”，避免一直卡着没人管。
- **行为**：审批/提问/错误未解决时，每隔 N 秒再响（可更急促/更高优先级），处理后才停。
- **可配置**：重复间隔、是否启用。
- **参考**：NotificationEngine 的边缘检测 + 自建计时器。
- **验收**：审批挂起 >30s 会再次提醒；处理掉后不再响。

#### C. 浏览器系统通知
- **需求**：dsh 在**后台标签页/最小化**时，声音可能被忽略。加**系统级通知**（浏览器 Notification API，需授权），确保切走也能收到。
- **行为**：触发提醒时，除声音外弹系统通知（标题+正文，正文可含 A 的“具体内容”）。当前会话可见时可按设置免打扰或仍提醒。
- **参考**：dsh-session-notification 的 `browser-notify.ts`（权限申请 + 通知展示）。
- **验收**：dsh 在后台时，触发审批会弹系统通知。

### P1 — 中价值

#### D. “免注视读输出”（朗读助手回复）
- **需求**：把助手的**回复**（或关键/报错）也读出来，用户“听”进度，彻底不盯屏。
- **实现**：接 TTS 引擎（浏览器 `speechSynthesis`，或更高质量的 Edge TTS —— 参考 dsh-plugin-tts）。可只读“最终回复”或“出错行”。
- **参考**：dsh-plugin-tts（Edge TTS + 自适应分块播放）。
- **验收**：完成一轮时自动朗读最终回复（可开关）。

#### E. 卡住 / 停滞检测
- **需求**：Agent 长时间无进展（如 2 分钟没新动作）就提醒，契合“别卡住”。
- **实现**：观察会话列表的活跃度/时间戳，超过阈值未变化则告警。
- **参考**：社区 `dsh-stall-guard`（停滞检测 + 记录）。
- **验收**：Agent 停顿超阈值时提示。

#### H. 插件 UI / 语音 i18n（多语言）—— 面向国际用户，治本
- **需求**：目前**界面（设置页、悬浮提示、每类/音色名）和语音朗读全是中文**。要真正面向国际用户，需要多语言。
- **范围**：
  1. 设置页全部文案：标题、音量、提醒范围、每类名称、音色名、按钮、提示。
  2. 悬浮提示文本（需要审批/需要回答/输出完成/发生错误/已连接）。
  3. 语音朗读语言（中文 / English，或跟随浏览器）。
  4. 音色名本地化（叮咚→Chime、低沉→Low、轻点→Tap、警醒→Alert）。
- **实现**：参考 dsh-plugin-tts 的 `t(key)` + zh/en 字典 + 语言偏好（auto/zh/en，存 localStorage）；或 dsh-session-notification 的 `ctx.locale.register` 命名空间 + `t`。
- **验收**：在设置里切到英文后，设置页、悬浮提示、语音朗读均为英文；中文用户不受影响。

### P2 — 锦上添花

#### F. 自定义音色上传
- **需求**：每类（或全局）上传自定义音频（mp3/ogg/wav），像 dsh-chime。
- **实现**：`FileReader.readAsDataURL` → 存 localStorage；`playType` 里 `sound === 'custom'` 时用 `new Audio(dataUrl)` 播放。
- **参考**：dsh-chime 的自定义声音导入 + `playCustom`。
- **验收**：上传后试听播放自定义音频，音量生效。

#### G. 语音语速 / 音色选择、静音（勿扰）时段
- 语音朗读可选语速/声音；可配“勿扰时段”（如某时间段不响）。
- **验收**：设置里能调语音语速/音色；勿扰时段静音。

#### I. 悬浮提示可开关（音频优先）
- **需求**：核心是“听”，悬浮提示（底部弹窗，3.6s 消失）对只听的人意义不大。
- **实现**：加一个“显示悬浮提示”开关，默认**关**（audio-first）；想看的人可开。
- **影响**：audio-first 定位——调高 A（播报具体内容）的优先级；提示仅作辅助。

---

## 其它可考虑
- **提醒范围可配置**：默认对所有会话，可在设置切成“仅当前会话”。
- **每条只响一次 vs 重复**：非阻断（完成）默认一次，阻断（审批/提问/错误）可重复。
- **完善 README 的“安装/使用/设置”说明**：随功能演进同步更新。

---

## 优先级建议
**先做 A（播报具体内容）+ B（重复提醒）+ C（系统通知）**——这三项最直接命中“不用看屏 + 别卡住”，且都能参考 dsh-session-notification / dsh-chime 的实现思路。若想**面向国际用户**，**H（i18n）** 也是关键——当前界面/语音全中文是“治标”层面的短板，i18n 属于“治本”。
