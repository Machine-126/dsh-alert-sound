# dsh-alert-sound bundle patch: one plugin row added to the host composition.
# The row's `id` MUST match `export const name` in lib/index.mjs.
# The row's `name` is the package name, resolved through Node after install.
- insert:
    - id: dsh-alert-sound
      name: 'dsh-alert-sound'
