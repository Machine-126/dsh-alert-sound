# dsh-alert-sound bundle patch: one plugin row added to the host composition.
# `id` names the plugin row (kept = the plugin's exported name).
# `name` is the package name, resolved through Node after install.
- insert:
    - id: dsh-alert-sound
      name: '@machine-126/dsh-alert-sound'
